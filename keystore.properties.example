# Copy to keystore.properties and fill in to produce signed release builds.
# Without this file, assembleRelease produces an unsigned APK, which is what
# F-Droid and other distributors build. Never commit the real file or the keystore.
storeFile=freeftp-release.jks
storePassword=
keyAlias=freeftp
keyPassword=
